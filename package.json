{
  "name": "pulse-pastures",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "cd frontend && npm run build && cd .. && rm -rf .next && cp -r frontend/.next .next",
    "start": "npm --prefix frontend run start"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.28.6"
  }
}
